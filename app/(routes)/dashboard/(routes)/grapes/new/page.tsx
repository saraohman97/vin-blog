import GrapeForm from "./(components)/form";

const GrapePage = () => {
  return (
    <div className="max-w-screen-lg mx-auto lg:py-10">
      <GrapeForm />
    </div>
  );
};

export default GrapePage;
